
export function filterNotes(notebooks: NotebookType[], tags: TagType[]): NoteType[] {
  let result: NoteType[] = []
  return result
  /*
  const selectedTags: TagType[] = getSelectedTags(tags);
  if (selectedTags.length == 0) {
    result = notes
  }
  else {
    result = notes.filter((note: NoteType) => hasTags(note, tags))
  }
  return result
  */
}
